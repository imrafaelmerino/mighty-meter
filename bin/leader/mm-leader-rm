#!/usr/bin/env bash

set -o pipefail
set -o nounset

# set -o xtrace

echo ""
echo "Executing script:$0 $@"
echo ""

main() {
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source ../.defaults
docker rm ${_NAME_LEADER_GRAFANA_CONTAINER}
docker rm ${_NAME_LEADER_INFLUXDB_CONTAINER}
docker rm ${_NAME_LEADER_NGINX_CONTAINER}
}

main "$@"