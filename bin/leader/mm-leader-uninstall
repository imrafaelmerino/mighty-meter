#!/usr/bin/env bash

main(){
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo "LEADER will be uninstalled, wanna continue (y/n)?"
read x
if [[ "$x" = "y" ]]
then
  ./mm-leader-stop
  ./mm-leader-rm
  ./mm-leader-rmi
  ./mm-leader-rm-vol
fi
}

main "$@"