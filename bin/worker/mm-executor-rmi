#!/usr/bin/env bash
set -o errexit
set -o pipefail
set -o nounset
# set -o xtrace
echo ""
echo "Executing script:$0 $@"
echo ""

main() {
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source ../.defaults
source ../.versions
source ../.defaults
docker rmi ${_NAME_WORKER_SERVER_IMAGE}:${_JIG_VERSION}
}

main "$@"