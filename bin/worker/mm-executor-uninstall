#!/usr/bin/env bash
#!/usr/bin/env bash

main(){
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo "JIG-EXECUTOR will be uninstalled, wanna continue (y/n)?"
read x
if [[ "$x" = "y" ]]
then
  mm-worker-stop
  mm-worker-rmi
  mm-worker-rm-vol
fi
}

main "$@"